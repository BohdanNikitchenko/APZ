@model IEnumerable<Sensor>
@{
    ViewData["Title"] = "ViewAllSensor";
}

<h1>ViewAllSensor</h1>

<main class="my-form">
    <div class="cotainer">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="panel panel-default panel-table">
                        <div class="panel-heading">
                            <div class="row">
                                <div class="col col-xs-6">
                                    <h3 class="panel-title">@SharedLocalizer["Sensors"]</h3>
                                </div>
                            </div>
                        </div>
                        <div class="panel-body">
                            <table class="table table-striped table-bordered table-list" id="dev-table">
                                <thead>
                                    <tr>
                                        <th style="width: 20%"><em class="fa fa-cog"></em></th>
                                        <th>@SharedLocalizer["SerialNumber"]</th>
                                        <th>@SharedLocalizer["SensorType"]</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var sensor in Model)
                                    {
                                        if (sensor.SerialNumber != null)
                                        {
                                            <tr>
                                                <td align="center">
                                                    <a href='@Url.Action("SensorPage", "Account", new { id = sensor.Id })' class="btn btn-default"><em class="fa fa-info"></em></a>
                                                    <a href='@Url.Action("SensorRemove", "Admin", new { id = sensor.Id })' class="btn btn-danger"><em class="fa fa-trash"></em></a>
                                                </td>
                                                <td>@sensor.SerialNumber</td>
                                                <td>@sensor.SensorType.Type</td>
                                            </tr>
                                        }
                                        else
                                        {
                                            <tr>
                                                <td align="center">
                                                    <a href='@Url.Action("SensorPage", "Account", new { id = sensor.Id })' class="btn btn-default"><em class="fa fa-info"></em></a>
                                                    <a href='@Url.Action("SensorRemove", "Admin", new { id = sensor.Id })' class="btn btn-danger"><em class="fa fa-trash"></em></a>
                                                </td>
                                                <td>@SharedLocalizer["SensorNull"]</td>
                                                <td>@sensor.SensorType.Type</td>
                                            </tr>
                                        }
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</main>


@*@foreach (var sensor in Model)
{
    if (sensor.SerialNumber != null)
    {
        <div>
            <br />
            <a href='@Url.Action("SensorPage","Account", new{id = sensor.Id})' class="link__dish">@sensor.SerialNumber</a>
            <a href='@Url.Action("SensorRemove","Admin",new{id = sensor.Id})' class="link__dish">Delete</a>
            <br />
        </div>
    }
    else
    {
        <div>
            <br />
            <a href='@Url.Action("SensorPage","Account", new{id = sensor.Id})' class="link__dish">@sensor.Id (delete by User)</a>
            <a href='@Url.Action("SensorRemove","Admin",new{id = sensor.Id})' class="link__dish">Delete</a>
            <br />
        </div>
    }
}*@
