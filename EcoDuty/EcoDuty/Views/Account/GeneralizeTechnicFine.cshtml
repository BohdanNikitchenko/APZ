@model IEnumerable<Technic>
@{
    ViewData["Title"] = "GeneralizeTechnicFine";
}
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.4.0/css/font-awesome.min.css" rel='stylesheet' type='text/css'>
<h1>GeneralizeTechnicFine</h1>

<main class="my-form">
    <div class="cotainer">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="panel panel-default panel-table">
                        <div class="panel-heading">
                            <div class="row">
                                <div class="col col-xs-6">
                                    <h3 class="panel-title">@Localizer["TechnicsFine"]</h3>
                                </div>

                            </div>
                        </div>
                        <div class="panel-body">
                            <table class="table table-striped table-bordered table-list">
                                <thead>
                                    <tr>
                                        <th style="width: 20%"><em class="fa fa-cog"></em></th>
                                        <th>@SharedLocalizer["AutoNumber"]</th>
                                        <th>@SharedLocalizer["SizeFine"]</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model != null)
                                    {
                                        foreach (var technic in Model)
                                        {
                                            if (technic.AutoNumber != null)
                                            {
                                                <tr>
                                                    <td align="center">
                                                        <a href='@Url.Action("TechnicPage","Account", new{id = technic.Id})' class="btn btn-default"><em class="fa fa-info"></em></a>
                                                    </td>
                                                    <td>@technic.AutoNumber</td>
                                                    <td>@technic.SizeFine</td>
                                                </tr>
                                            }
                                            else
                                            {
                                                <tr>
                                                    <td align="center">
                                                        <a href='@Url.Action("TechnicPage","Account", new{id = technic.Id})' class="btn btn-default"><em class="fa fa-info"></em></a>
                                                    </td>
                                                    <td>@SharedLocalizer["TechnicNull"]</td>
                                                    <td>@technic.SizeFine</td>
                                                </tr>
                                            }
                                        }
                                    }

                                </tbody>
                            </table>
                            <div>
                                <a href="@Url.Action("Account","Account")">@SharedLocalizer["Back"]</a>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</main>

@*@foreach (var technic in Model)
{
    if (technic.AutoNumber != null)
    {
        <div>
            <br />
            <a href='@Url.Action("TechnicPage","Account", new{id = technic.Id})' class="link__dish">
                <p>Соответствия нормам экологических стандартов</p>
                <p>регистрационный номер техники @technic.AutoNumber</p>
                <p>Количество штрафных баллов @technic.SizeFine</p>
            </a>
            <br />
        </div>
    }
    else
    {
        <div>
            <br />
            <a href='@Url.Action("TechnicPage","Account", new{id = technic.Id})' class="link__dish">
                <p>Соответствия нормам экологических стандартов</p>
                <p>техника была удаленна</p>
                <p>Количество штрафных баллов @technic.SizeFine</p>
            </a>
            <br />
        </div>
    }

}*@